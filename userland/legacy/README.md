# Legacy userland

This directory keeps pre-newlib userland sources for reference only.

The active shell and user commands are built from `userland/newlib-tests/`.
Do not modify the legacy shell for current ArmOS behavior.
